# Job Portal

A modern job portal tracking application built with React, Vite, and Tailwind CSS.

## Getting Started

1. Install dependencies: `npm install`
2. Run the development server: `npm run dev`
3. Build for production: `npm run build`
